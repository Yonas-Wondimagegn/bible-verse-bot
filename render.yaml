services:
  - type: worker
    name: bible-verse-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bible_verse_bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
